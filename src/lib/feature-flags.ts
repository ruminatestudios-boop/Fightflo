/**
 * Core-loop shipping mode.
 * Set to `true` to restore the full FightFlo experience.
 */
export const SHOW_ADVANCED = true;

/** Use style-specific move callouts instead of generic attack/defend/move signals */
export const USE_MOVE_CALL_ENGINE = true;
